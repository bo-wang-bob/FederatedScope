# 单机准确率平台（第一阶段）

## 部署与隔离

- 分支：`codex/single-host-platform`，独立工作树。原分支、原服务和原数据均保留。
- 系统/训练机器：4090lziy，10.112.81.135；默认 GPU 1，可选择 CPU 或其他实际 GPU。
- 新入口：`federatedscope.standalone_api.platform_app`，127.0.0.1:8001。
- 原 8000 服务不替换。新服务通过 SSH 转发访问，例如本机 18001 -> 服务器 8001。
- 单机进程内模拟客户端，不启动子服务器或训练网络端口。默认每次只接收一个预检、训练或评测任务。
- 服务配置见 `scripts/federatedscope-platform.service`；API 没有用户身份认证，勿直接暴露公网。

## 已打通

配置 -> 异步逐样本预检 -> 单机联邦训练 -> 实时指标/客户端监控 -> final/best 模型 -> 冻结测试包独立评测 -> 对比与 JSON/CSV/完整复现包导出。

前端沿用原 AppShell、品牌、面板、指标卡与图表组件，保留深蓝指挥舱视觉，增加更清晰的布局、真实逻辑拓扑、可读字号和响应式布局。不使用旧模拟地图、虚构进度或恶意节点真值。

专家可选择平台已完成训练的模型和兼容测试集，进一步选域/类别。评测输出总体、分域、每类 Precision/Recall/F1、Macro-F1 和混淆矩阵。测试集与模型必须匹配特征空间，跨实验测试还会检查训练样本交叉。现有任意外部模型尚不自动导入，避免无元数据模型被错误评测。

## 参数与评测语义

- 页面“通信轮数”表示实际模型更新次数，不包含初始化轮 0；底层 `total_round_num = rounds + 1`。
- 所有客户端先完成缓存准备。每轮参与数为 0 时全部参与；否则按记录的随机种子确定性抽取客户端，每轮独立抽取。
- 本地轮数、学习率、batch size、客户端数、训练种子、数据划分种子、alpha、样本上限和评测频率均绑定到实际配置。
- Digits 的现有固定划分锁定为 60 客户端、alpha=0.1、splitSeed=42；不接受不会生效的划分参数。
- `samplesPerClient=0` 使用全部客户端原始缓存；正数启用原实现的确定性抽样（可能有放回），分布与训练样本数有记录。
- 普通算法统一禁用增强数据生成；保留各源 YAML 的 FedProx、FedProto、FedOpt、MOON 设置。旧 YAML 中部分“FedAvg”启用了生成，平台不会沿用这项误配置。
- 训练仍使用原算法和所选 GPU。所有训练期测试与独立评测统一使用 CPU float32、batch 256、2 线程，避免 CPU/GPU LSTM 近似相同 logits 在边界样本上产生不同预测。
- 总体准确率按样本加权；分域平均按域等权。Macro 指标按真值或预测中出现的类别等权。最终轮强制评测。
- `best` 基于训练期测试指标选取，不能视为无偏泛化估计；默认建议 `final`。

## 缓存与复现

- 原资源目录只复用，不下载主干、不提取特征、不生成增强样本。缺失训练样本、测试缓存、维度/标签不匹配时明确失败。
- 可用 `FS_PLATFORM_CACHE_<GROUP大写>` 指定某组的既有训练缓存目录，`FS_PLATFORM_TEST_CACHE_<GROUP大写>` 单独指定既有测试缓存。目录写入复现记录，仍须通过样本、完整标签序列校验，不会复制覆盖原缓存。
- 部分旧测试缓存没有样本 ID：按原缓存文件命名中的模型/划分/种子、全部样本数及完整标签顺序进行绑定。此类证据弱于内嵌样本 ID，限制写入 `testProvenance`，不会宣称逐行原始来源已得到加密证明。
- 记录请求、实际解析配置、参数绑定、划分/缓存哈希、原始样本清单、运行环境、Git 提交、实际 Python 源码快照、模型、测试特征、日志和每轮参与集合。
- 预检后再次验证缓存和划分指纹。改参必须重预检。接口对未知字段、非法值、重复请求及并发冲突明确拒绝。

## 停止与恢复

- 仅根据该任务 spec 绝对路径、PID 与创建时间识别并回收进程/其子进程；不按端口、Python 名称或 GPU 号批量杀进程。
- 停止保留日志与部分产物；失败产物不进入模型库。清理未成功会阻止新任务并提供重试。
- 重启后回收遗留的本平台任务，恢复任务历史并标为中断。不会假装从缺少完整客户端优化器状态的模型继续训练；可从页面复用配置重新预检、重跑。
- 原系统、其他工作树、GPU 上的无关任务均不参与清理。

## 验证

```sh
python -m unittest discover -s tests -p test_single_host_platform.py -v
python scripts/platform_acceptance.py --group officehome_vit --rounds 2
python scripts/platform_acceptance.py --group digit3_vit --rounds 2
python scripts/platform_acceptance.py --group mdsent_lstm --rounds 2
cd frontend && npm test && npm run build
```

后端测试覆盖参数映射、固定划分约束、幂等/并发、停止时序、清理失败、重启回收、无关进程隔离、指标计算、独立评测和模型哈希篡改。前端交互测试核对可见参数进入请求以及改参使预检失效。

真实验收是短轮数功能检查，不是准确率达标报告。尚未核验所有数据配置和算法组合的完整长训练。GGEUR 增强缓存缺少已登记的可核验完整划分来源，当前明确禁用，不临时生成。隐私与后门不扩展。

2026-09-09：前端 23 项、后端 13 项测试通过；Office-Home ViT/FedProx、Digits ViT/FedAvg、MDSent LSTM/FedAvg 的真实短训练与模型重载评测指标一致。DomainNet ViT 原目录缺少匹配测试缓存，另存的 20260907 label_correction 测试缓存也未通过当前划分校验，未设为默认或跳过校验。

实际 HTTP 验收还通过了 MDSent/books/类别 0 子集评测（48 样本）、JSON/CSV/模型/复现包下载、任务停止和新服务主进程异常退出后的自动恢复。恢复测试任务被标为 `interrupted`，清理成功；原 8000 健康接口始终返回 200。短训练和中断验收记录均保留在新平台任务列表。
