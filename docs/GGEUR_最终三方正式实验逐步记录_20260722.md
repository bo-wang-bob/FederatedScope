# GGEUR 最终三方正式实验逐步记录

## 记录规则

- 实验时区：Asia/Shanghai（UTC+08:00）。
- 每条记录包含：时间、机器、工作目录、命令、退出状态和关键结果。
- 所有实验严格串行；上一项三端进程和端口清理完成后才启动下一项。
- SSH 密码、私钥内容及其他认证凭据不写入本文档；命令中的认证部分统一记作 `<SSH_AUTH_REDACTED>`。
- 三机业务通信必须使用真实内网地址和端口，不使用本机回环代替实验链路。

## 固定拓扑

| 层级 | 机器 | 工作目录 | 业务端口 | 职责 |
| --- | --- | --- | --- | --- |
| 客户端层 | `LKX` / `10.129.222.189` / Windows | `D:\Projects\FederatedScope` | `20000-20059/TCP` | 60 个独立 OfficeHome 客户端 |
| 子服务器层 | `DESKTOP-UB9PJQU` / `10.129.248.111` / Windows | `C:\Users\pc\FederatedScope` | `61000-61001/TCP` | 2 个子服务器，各聚合 30 个客户端 |
| 根服务器层 | `10.112.81.135` / Linux | `/root/autodl-tmp/FederatedScope` | `60050/TCP` | 根聚合与全局评估，使用 GPU 1 |

## 最终会话：ViT + OfficeHome

实验顺序：FedAvg → FedProx → FedProto → FedOpt → MOON → GGEUR。

### 步骤 0：建立记录并核对本地配置

- 时间：2026-07-22 22:41:58 +08:00
- 执行机器：本地 Windows
- 工作目录：`D:\Projects\FederatedScope`
- 命令：

  ```powershell
  Get-Content scripts\distributed_scripts\ggeur_hierarchical_3machine\README.md
  Get-ChildItem scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5 -Directory
  git status --short
  ```

- 退出状态：0
- 结果：确认存在 `officehome_vit_fedavg`、`officehome_vit_fedprox`、`officehome_vit_fedproto`、`officehome_vit_fedopt`、`officehome_vit_moon`、`officehome_vit_ggeur` 六项配置。工作树包含本次分布式改造，未执行覆盖或清理用户改动。

### 步骤 1：本地到 8G SSH 登录检查

- 时间：2026-07-22 22:42 +08:00
- 执行机器：本地 Windows
- 目标机器：8G `LKX`
- 命令（认证信息脱敏）：

  ```powershell
  ssh <SSH_AUTH_REDACTED> fsuser@2001:da8:215:3c0a:f51:4d71:80:8075 cmd /c hostname
  ```

- 退出状态：0
- 关键输出：`LKX`

### 步骤 2：8G 到另外两台机器的真实网络预检

- 时间：2026-07-22 22:42 +08:00
- 执行机器：8G `LKX`
- 命令：

  ```powershell
  Test-NetConnection 10.129.248.111 -Port 22 -InformationLevel Quiet
  Test-NetConnection 10.112.81.135 -Port 22 -InformationLevel Quiet
  ```

- 退出状态：两条均为 0
- 关键输出：两条均为 `True`
- 说明：这是 8G 经真实内网分别连接第三台 Windows 和 Linux 根服务器，不是回环测试。

### 步骤 3：认证预检中的非破坏性失败记录

- 时间：2026-07-22 22:42 +08:00
- 执行机器：8G `LKX`
- 命令：

  ```powershell
  ssh -o BatchMode=yes pc@10.129.248.111 cmd /c hostname
  ssh -o BatchMode=yes root@10.112.81.135 hostname
  ```

- 退出状态：两条均为 1
- 关键输出：`Permission denied`
- 结论：两台目标机允许 SSH，但尚未为 8G 配置无密码公钥认证；正式控制使用会话内凭据，凭据不落盘、不进入实验日志。该检查没有启动或修改远端进程。

### FedAvg：已完成的首项正式基线

- 启动：2026-07-22 22:14 +08:00
- 完成：2026-07-22 22:30 +08:00
- 根服务器启动命令：

  ```bash
  cd /root/autodl-tmp/FederatedScope
  bash scripts/distributed_scripts/ggeur_hierarchical_3machine/launch_root.sh \
    scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_fedavg
  ```

- 第三台子服务器启动命令：

  ```powershell
  Set-Location C:\Users\pc\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_subservers.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedavg `
    -PythonBin C:\Users\pc\miniconda3\envs\cerp\python.exe `
    -UseScheduledTasks
  ```

- 8G 客户端启动命令：

  ```powershell
  Set-Location D:\Projects\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_clients.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedavg `
    -UseScheduledTasks
  ```

- 运行证据：60/60 客户端加入；两个子服务器每轮分别聚合 30/30；99/99 个训练轮均在根服务器收到 2/2 子服务器更新。
- 结果：最终/最佳平均准确率均为 39.33%；Art 40.33%，Clipart 29.54%，Product 42.79%，Real_World 44.65%。
- 运行时长：15 分 56 秒。
- 本地原始日志：`docs/test_logs/officehome_vit_three_machine_20260721_v5/officehome_vit_fedavg/root.stdout.log`。
- 清理结果：三端实验进程、计划任务和监听端口均已清理；缓存与日志保留。

### FedProx：待启动

### 步骤 4：三端正式启动前的干净状态检查

- 时间：2026-07-22 22:45:15 至 22:45:16 +08:00
- 8G `LKX`：
  - FedProx 配置存在：`True`
  - ViT + OfficeHome 完整特征缓存标记存在：`True`
  - `20000-20059` 监听端口数：`0`
  - `GGEUR-officehome_vit_*` 计划任务数：`0`
- 第三台 `DESKTOP-UB9PJQU`：

  ```powershell
  Set-Location C:\Users\pc\FederatedScope
  Test-Path scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedprox
  @(Get-NetTCPConnection -LocalPort 61000,61001 -State Listen -ErrorAction SilentlyContinue).Count
  @(Get-ScheduledTask -TaskName 'GGEUR-officehome_vit_*' -ErrorAction SilentlyContinue).Count
  ```

  结果：配置存在；监听端口数 `0`；计划任务数 `0`。
- 根服务器 `ct-2x4090-lziy`：

  ```bash
  cd /root/autodl-tmp/FederatedScope
  test -d scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_fedprox
  test -d /root/autodl-tmp/datasets/OfficeHomeDataset_10072016
  test -f /root/.cache/clip/ViT-B-16.pt
  ss -ltnH 'sport = :60050' | wc -l
  pgrep -af 'officehome_vit_.*root|run_ggeur' | grep -v grep | wc -l
  ```

  结果：配置、数据集、ViT 权重均存在；根端口数 `0`；根训练进程数 `0`。
- 结论：三端没有上一项实验残留，允许启动 FedProx。

### FedProx：启动中

#### 步骤 5：根服务器首次启动失败（已保留）

- 时间：2026-07-22 22:45 至 22:46 +08:00
- 执行机器：根服务器 `ct-2x4090-lziy`
- 首次命令：

  ```bash
  cd /root/autodl-tmp/FederatedScope
  bash scripts/distributed_scripts/ggeur_hierarchical_3machine/launch_root.sh \
    scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_fedprox
  ```

- 退出状态：1
- 原因：SSH 非交互会话默认 `python3` 中没有 PyTorch，根日志报 `ModuleNotFoundError: No module named 'torch'`。
- 影响：根端没有监听；此时尚未启动子服务器和客户端。
- 环境验证命令：

  ```bash
  /root/.local/share/mamba/envs/GGEUR/bin/python -c \
    "import torch,sys; print(sys.executable); print(torch.__version__)"
  ```

- 结果：解释器路径正确，PyTorch 为 `2.5.1+cu121`。

#### 步骤 6：显式指定正式 Python 环境后启动根服务器

- 时间：2026-07-22 22:46 +08:00
- 执行机器：根服务器 `ct-2x4090-lziy`
- 命令：

  ```bash
  cd /root/autodl-tmp/FederatedScope
  PYTHON_BIN=/root/.local/share/mamba/envs/GGEUR/bin/python \
    bash scripts/distributed_scripts/ggeur_hierarchical_3machine/launch_root.sh \
    scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_fedprox
  ```

- 退出状态：0
- 结果：`root_pid=8464`，`root_port=60050 ready=true`。

#### 步骤 7：启动第三台的两个子服务器

- 时间：2026-07-22 22:46 +08:00
- 执行机器：第三台 `DESKTOP-UB9PJQU`
- 命令：

  ```powershell
  Set-Location C:\Users\pc\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_subservers.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedprox `
    -PythonBin C:\Users\pc\miniconda3\envs\cerp\python.exe `
    -UseScheduledTasks
  ```

- 退出状态：0
- 结果：
  - 子服务器 1：PID `20424`，端口 `61000`，任务 `GGEUR-officehome_vit_fedprox-subserver_1`。
  - 子服务器 2：PID `20512`，端口 `61001`，任务 `GGEUR-officehome_vit_fedprox-subserver_2`。
- 业务链路复查：根服务器到 `10.129.248.111:61000/61001` 均成功；第三台到 `10.112.81.135:60050` 为 `True`。

#### 步骤 8：8G 客户端首次启动失败（已保留）

- 时间：2026-07-22 22:47 +08:00
- 执行机器：8G `LKX`
- 命令：

  ```powershell
  Set-Location D:\Projects\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_clients.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedprox `
    -UseScheduledTasks
  ```

- 退出状态：1；脚本在客户端 1 停止，其余 59 个尚未启动。
- 根因：轻量导入模式未导出 FedProx 包装器，错误为 `ImportError: cannot import name 'wrap_fedprox_trainer'`。
- 清理命令：

  ```powershell
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\stop_clients.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedprox `
    -Force
  ```

- 清理结果：失败客户端对应进程、PID 文件和计划任务已清理。

#### 步骤 9：本地修复、验证和三端同步

- 修复文件：`federatedscope/core/trainers/__init__.py`。
- 修复内容：轻量模式只额外导入 FedProx 所需的 `wrap_fedprox_trainer`，不恢复 TensorFlow 等无关重型导入。
- 本地验证命令：

  ```powershell
  python -m py_compile federatedscope\core\trainers\__init__.py tests\test_ggeur_hierarchical.py
  $env:FEDERATEDSCOPE_GGEUR_LIGHTWEIGHT='1'
  python -c "from federatedscope.core.trainers import wrap_fedprox_trainer; assert callable(wrap_fedprox_trainer)"
  ```

- 结果：编译通过；`lightweight_fedprox_import=PASS`。本机 Python 未安装 pytest，因此没有伪报 pytest 通过。
- 三端覆盖前备份目录：`.codex_backups/20260722_224924`（各机器仓库内对应路径）。
- 同步后文件 SHA-256：`1A705A547EBE6D539FDF3756E864786519C5F01D9644440FFCE49D46D5F2B421`，三端一致。

#### 步骤 10：重新启动 8G 的 60 个客户端

- 时间：2026-07-22 约 22:50 至 22:54 +08:00
- 执行机器：8G `LKX`
- 命令：与步骤 8 相同。
- 退出状态：0
- 结果：`client_000001` 至 `client_000060` 全部 `ready=true`，监听端口完整覆盖 `20000-20059`，`started_clients=60`；状态脚本随后确认 `running=60 stopped=0 total=60`。

#### 步骤 11：正式训练启动后核对

- 时间：2026-07-22 22:54 至 22:55 +08:00
- 8G：60/60 客户端进程和计划任务均为 `Running`。
- 第三台：两个子服务器进程和计划任务均为 `Running`。
- 根服务器：第 1 至第 10 训练轮已完成；每轮 `valid_updates=2/2`，第 10 轮平均准确率为 16.39%。
- 当前状态：FedProx 正式训练中，继续逐轮监控。

后续轮次、异常、完成准确率和清理结果将在实际执行时按时间顺序追加。

#### 步骤 12：逐轮监控

- 时间：2026-07-22 22:54 至 23:04 +08:00
- 执行机器：根服务器 `ct-2x4090-lziy`
- 每 20 秒检查的命令逻辑：

  ```bash
  grep -a 'aggregation complete' logs/root.stdout.log | tail -n 1
  grep -aEc 'Traceback| ERROR:' logs/root.stdout.log
  kill -0 8464
  ```

- 检查点：第 36、48、61、73、84、95、99 轮均正常。
- 最终结果：99 个训练轮全部完成；99/99 行均为 `valid_updates=2/2`；根日志错误数 `0`。

#### 步骤 13：结果固化

- 完成时间：2026-07-22 23:04:46 +08:00
- 根日志结果：
  - Art：最终 46.78%，最佳 46.91%。
  - Clipart：最终/最佳 37.10%。
  - Product：最终/最佳 51.43%。
  - Real_World：最终/最佳 54.74%。
  - 平均：最终 47.51%，最佳 47.53%。
  - 总训练时间：18 分 24 秒（1104.7 秒）。
  - 平均轮时 6.5 秒，最短 5.8 秒，最长 9.1 秒。
  - overall train QPS：3253.03 samples/s；stable average QPS：3264.04 samples/s。
  - 通信发送量：755.57 MB。
- 本地提取命令：

  ```powershell
  python scripts\distributed_scripts\ggeur_hierarchical_3machine\summarize_accuracy.py `
    docs\test_logs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedprox\root.stdout.log `
    --output docs\test_logs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedprox\accuracy_summary.json
  ```

- 本地根日志 SHA-256：`FD78D4A8DDE9E899CE110C94F4DD3BB8419852E7EDBF6D81DFB15012DC86076F`，与远端一致。
- 8G 客户端日志错误扫描：`0`。
- 第三台子服务器日志错误扫描：`0`。

#### 步骤 14：三端清理

- 8G：

  ```powershell
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\stop_clients.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedprox `
    -Force
  ```

- 第三台：

  ```powershell
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\stop_subservers.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedprox `
    -Force
  ```

- 根服务器：

  ```bash
  bash scripts/distributed_scripts/ggeur_hierarchical_3machine/stop_role.sh \
    scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_fedprox \
    root true
  ```

- 清理后验证：
  - 8G `20000-20059` 监听数 `0`，FedProx 计划任务数 `0`。
  - 第三台 `61000-61001` 监听数 `0`，FedProx 计划任务数 `0`。
  - 根服务器 `60050` 监听数 `0`，相关进程数 `0`，根 PID 文件数 `0`。

### FedProx：完成

- 正式结果有效，异常与修正均已保留。
- 下一项：FedProto；必须在重新完成三端干净状态检查后启动。

## FedProto 正式实验

### 步骤 15：启动三端

- 启动时间：2026-07-22 23:08:54 +08:00
- 根服务器命令：

  ```bash
  cd /root/autodl-tmp/FederatedScope
  PYTHON_BIN=/root/.local/share/mamba/envs/GGEUR/bin/python \
    bash scripts/distributed_scripts/ggeur_hierarchical_3machine/launch_root.sh \
    scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_fedproto
  ```

- 根结果：PID `8989`，端口 `60050 ready=true`。
- 第三台命令：

  ```powershell
  Set-Location C:\Users\pc\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_subservers.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedproto `
    -PythonBin C:\Users\pc\miniconda3\envs\cerp\python.exe `
    -UseScheduledTasks
  ```

- 第三台结果：子服务器 PID `5188`/`20396`，端口 `61000`/`61001` 均 ready。
- 8G 命令：

  ```powershell
  Set-Location D:\Projects\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_clients.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedproto `
    -UseScheduledTasks
  ```

- 8G 结果：60 个客户端全部 `ready=true`，`started_clients=60`。

### 步骤 16：第 0 轮和逐轮监控

- 统计量：60/60 客户端全部到达；聚合 65 类协方差和全局原型。
- 全局原型/协方差分发：60/60 客户端完成。
- 增强就绪：60/60 客户端完成。
- 训练检查点：第 1、11、20、31、41、51、60、71、81、92、99 轮均正常。
- 99/99 个训练轮均为 `valid_updates=2/2`，根日志错误数始终为 0。

### 步骤 17：结果固化与清理

- 完成时间：2026-07-22 23:32:25 +08:00。
- 最终/最佳平均准确率：62.10% / 62.10%。
- 域准确率：Art 63.24%，Clipart 51.45%，Product 66.29%，Real_World 最终 67.43%/最佳 67.58%。
- 总训练时间：23 分 30 秒；平均轮时 6.5 秒。
- overall train QPS：4029.53 samples/s；stable average QPS：4041.25 samples/s。
- 根日志 SHA-256：`79741495BF8E62D280F2100EA71CAAAEEC13804CD022C6BC73A1DDEFAB5B3BE2`，本地与远端一致。
- 客户端、子服务器、根日志错误扫描均为 0。
- 清理命令与 FedProx 相同，仅将 case 替换为 `officehome_vit_fedproto`。
- 清理后：8G 客户端端口 0；第三台端口/任务 0；根端端口/进程 0。

### FedProto：完成

- 下一项：FedOpt。

## FedOpt 正式实验

- 启动时间：2026-07-22 23:35:12 +08:00；完成时间：23:49:54。
- 三端启动命令与前述相同，case 替换为 `officehome_vit_fedopt`。
- 根服务器：PID `9812`，端口 `60050` ready。
- 第三台：PID `16304`/`14608`，端口 `61000`/`61001` ready。
- 8G：60 个客户端、`20000-20059` 全部 ready。
- 监控检查点：第 1、14、24、33、43、53、63、72、82、92、99 轮均正常；99/99 轮 `valid_updates=2/2`，错误数始终为 0。
- 根日志明确记录 `Performing FedOpt aggregation`。
- 最终/最佳平均准确率：39.44% / 39.44%。
- 域准确率：Art 40.33%，Clipart 29.69%，Product 42.94%，Real_World 44.80%。
- 总训练时间：14 分 41 秒；overall train QPS 4030.50 samples/s。
- 根日志 SHA-256：`874FCE344665EE5364E10CE8DF1DBDF40A29885C089685B9C92EC478B93F30A2`。
- 三端错误扫描为 0；清理后客户端端口、子服务器端口/任务、根端口/进程均为 0。

### FedOpt：完成

- 下一项：MOON。

## MOON 正式实验

- 启动时间：2026-07-22 23:52:48 +08:00；完成时间：2026-07-23 00:08:27。
- 三端启动命令的 case 为 `officehome_vit_moon`。
- 根服务器 PID `10389`；第三台子服务器 PID `18136`/`13272`；8G 60 个客户端全部 ready。
- 60/60 客户端增强就绪。
- 监控检查点：第 1、9、17、24、31、41、51、59、69、79、88、98、99 轮均正常。
- 99/99 轮 `valid_updates=2/2`，错误数始终为 0。
- 最终/最佳平均准确率：39.33% / 39.33%；域结果为 40.33%、29.54%、42.79%、44.65%。
- 总训练时间 15 分 39 秒；overall train QPS 3713.74 samples/s。
- 根日志 SHA-256：`B2C8213231F2627CFEAE7660405849EDC6DF50735EA046F1BB50CC67915ADF5F`。
- 三端错误扫描为 0；清理后端口、任务和进程均为 0。

### MOON：完成

- 下一项：GGEUR。

## GGEUR 正式实验

### 步骤 18：按根 → 子服务器 → 客户端顺序启动三端

- 根服务器启动时间：2026-07-23 00:10:59 +08:00。
- 执行机器：4090 Linux 根服务器 `10.112.81.135`。
- 工作目录：`/root/autodl-tmp/FederatedScope`。
- 实际命令：

  ```bash
  cd /root/autodl-tmp/FederatedScope
  PYTHON_BIN=/root/.local/share/mamba/envs/GGEUR/bin/python \
    bash scripts/distributed_scripts/ggeur_hierarchical_3machine/launch_root.sh \
    scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_ggeur
  ```

- 结果：根 PID `11019`，真实业务端口 `10.112.81.135:60050` ready。
- 执行机器：第三台 Windows `DESKTOP-UB9PJQU` / `10.129.248.111`。
- 工作目录：`C:\Users\pc\FederatedScope`。
- 实际命令：

  ```powershell
  Set-Location C:\Users\pc\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_subservers.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_ggeur `
    -PythonBin C:\Users\pc\miniconda3\envs\cerp\python.exe `
    -UseScheduledTasks
  ```

- 结果：子服务器 PID `19652`/`4368`，真实业务端口 `10.129.248.111:61000`/`61001` ready。
- 执行机器：8G Windows `LKX` / `10.129.222.189`。
- 工作目录：`D:\Projects\FederatedScope`。
- 实际命令：

  ```powershell
  Set-Location D:\Projects\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_clients.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_ggeur `
    -UseScheduledTasks
  ```

- 结果：`client_000001` 至 `client_000060` 全部 ready，真实业务端口完整覆盖 `10.129.222.189:20000-20059`。
- 说明：SSH 隧道仅用于管理登录；根、子服务器和客户端之间的训练通信全部使用上面的真实 10.x IP 和业务端口。

### 步骤 19：第 0 轮与第 1-99 轮监控

- 第 0 轮：60/60 客户端统计量到达，聚合 65 类全局原型/协方差；60/60 完成分发；60/60 增强就绪。
- 第 1-99 轮：99/99 轮均为 `valid_updates=2/2`，根日志错误计数始终为 0。
- 根端实际监控命令逻辑：

  ```bash
  grep -a 'aggregation complete' logs/root.stdout.log | tail -n 1
  grep -aEc 'Traceback| ERROR:' logs/root.stdout.log
  kill -0 11019
  ```

- 完成时间：2026-07-23 00:34:00 +08:00。
- 最终/最佳平均准确率：76.70% / 76.70%。
- 域准确率：Art 最终 72.02%/最佳 72.15%；Clipart 64.27%；Product 最终 86.56%/最佳 86.71%；Real_World 83.94%。
- 总训练时间：23 分 00 秒（1380.6 秒，含第 0 轮）；平均轮时 6.2 秒，最短 5.8 秒，最长 6.8 秒。
- 总训练样本处理量：19,305,000；overall train QPS 31,249.92 samples/s；stable average QPS 31,311.97 samples/s；通信发送量 755.57 MB。

### 步骤 20：日志回收、哈希和错误扫描

- 4090 原始根日志：
  `/root/autodl-tmp/FederatedScope/scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_ggeur/logs/root.stdout.log`。
- 本地副本：
  `docs/test_logs/officehome_vit_three_machine_20260721_v5/officehome_vit_ggeur/root.stdout.log`。
- 本地准确率提取命令：

  ```powershell
  python scripts\distributed_scripts\ggeur_hierarchical_3machine\summarize_accuracy.py `
    docs\test_logs\officehome_vit_three_machine_20260721_v5\officehome_vit_ggeur\root.stdout.log `
    --output docs\test_logs\officehome_vit_three_machine_20260721_v5\officehome_vit_ggeur\accuracy_summary.json
  ```

- 本地与远端根日志 SHA-256 一致：`1487AA5A3D3E43F1E87FDE2852F9EFB6731033D8CCA1B3EA4B54A3674BAA4F7A`。
- 8G：60 个 `client_*.stderr.log` 文件，`Traceback`/` ERROR:` 匹配数 `0`。
- 第三台：2 个 `subserver_*.stderr.log` 文件，`Traceback`/` ERROR:` 匹配数 `0`。
- 根日志：99/99 训练轮、99/99 `valid_updates=2/2`，错误匹配数 `0`。

### 步骤 21：三端停止和独立清场复核

- 8G 实际停止命令：

  ```powershell
  Set-Location D:\Projects\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\stop_clients.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_ggeur `
    -Force
  ```

- 第三台实际停止命令：

  ```powershell
  Set-Location C:\Users\pc\FederatedScope
  .\scripts\distributed_scripts\ggeur_hierarchical_3machine\stop_subservers.ps1 `
    -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_ggeur `
    -Force
  ```

- 4090 实际停止命令：

  ```bash
  cd /root/autodl-tmp/FederatedScope
  bash scripts/distributed_scripts/ggeur_hierarchical_3machine/stop_role.sh \
    scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_ggeur \
    root true
  ```

- 清场复核结果：
  - 8G：`20000-20059` 监听端口 `0`，客户端进程 `0`，计划任务 `0`，客户端 PID 文件 `0`。
  - 第三台：`61000-61001` 监听端口 `0`，子服务器进程 `0`，计划任务 `0`，子服务器 PID 文件 `0`。
  - 4090：`60050` 监听端口 `0`，根进程 `0`，根 PID 文件 `0`。

### GGEUR：完成

- ViT + OfficeHome 六种方法已按顺序全部完成，原始日志、准确率 JSON、逐机命令和异常修复记录均已保留。

## FedOpt 与 MOON 实际启动命令补全

为避免“同前、仅替换 case”产生审计歧义，以下补全两项当时实际使用的三端命令。执行顺序均为 4090 根服务器 → 第三台子服务器 → 8G 客户端。

### FedOpt

```bash
cd /root/autodl-tmp/FederatedScope
PYTHON_BIN=/root/.local/share/mamba/envs/GGEUR/bin/python \
  bash scripts/distributed_scripts/ggeur_hierarchical_3machine/launch_root.sh \
  scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_fedopt
```

```powershell
Set-Location C:\Users\pc\FederatedScope
.\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_subservers.ps1 `
  -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedopt `
  -PythonBin C:\Users\pc\miniconda3\envs\cerp\python.exe `
  -UseScheduledTasks
```

```powershell
Set-Location D:\Projects\FederatedScope
.\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_clients.ps1 `
  -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_fedopt `
  -UseScheduledTasks
```

### MOON

```bash
cd /root/autodl-tmp/FederatedScope
PYTHON_BIN=/root/.local/share/mamba/envs/GGEUR/bin/python \
  bash scripts/distributed_scripts/ggeur_hierarchical_3machine/launch_root.sh \
  scripts/distributed_scripts/ggeur_hierarchical_3machine/runs/officehome_vit_three_machine_20260721_v5/officehome_vit_moon
```

```powershell
Set-Location C:\Users\pc\FederatedScope
.\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_subservers.ps1 `
  -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_moon `
  -PythonBin C:\Users\pc\miniconda3\envs\cerp\python.exe `
  -UseScheduledTasks
```

```powershell
Set-Location D:\Projects\FederatedScope
.\scripts\distributed_scripts\ggeur_hierarchical_3machine\launch_clients.ps1 `
  -CaseDir scripts\distributed_scripts\ggeur_hierarchical_3machine\runs\officehome_vit_three_machine_20260721_v5\officehome_vit_moon `
  -UseScheduledTasks
```

认证参数与口令在本审计文件中统一脱敏；它们未写入仓库、配置或实验日志。
