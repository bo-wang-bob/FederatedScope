# 下一阶段前后端实时联动优化计划

## 1. 目标与范围

下一阶段从“前端演示数据驱动”转为“单机训练进程真实驱动”。前端负责配置实验、启动或停止任务、订阅训练事件并展示结果；后端继续采用单机运行方式，不实现真实分布式客户端，也不实现真实的中央服务器—域子服务器两级聚合。地图中的三级结构仍用于表达跨域联邦学习过程。

本阶段聚焦五个页面：

1. 综合态势首页。
2. 场景与异构分析。
3. 实验配置。
4. 运行监控。
5. 实验记录。

隐私攻击效果、对照分析和系统设置三个独立页面不在本阶段实现。隐私实验和后门实验继续分别运行，后门实验不叠加隐私攻击或隐私保护。

## 2. 页面与路由调整

| 路由 | 下一阶段处理 | 页面职责 |
| --- | --- | --- |
| `/overview` | 保留 | 全屏地图、四域节点和三级数据流总览 |
| `/scenario-analysis` | 保留并精简 | 设置基础异构环境，查看每域、每客户端的数据分布 |
| `/experiments/new` | 保留并重构 | 配置、导出、启动异构协同、隐私保护或后门攻防实验 |
| `/experiments/:id/live` | 保留并重构 | 连接真实任务，展示进度、事件和模式相关指标，支持停止任务 |
| `/reports` | 保留并改名为“实验记录” | 记录每次实验及其最终状态、配置摘要和结果 |
| `/experiments/:id/privacy` | 移除 | 本阶段不实现独立隐私攻击效果页 |
| `/experiments/:id/compare` | 移除 | 本阶段不实现独立对照分析页 |
| `/settings` | 移除 | 本阶段不实现系统设置页 |

旧地址 `/scenario` 和 `/heterogeneity` 可继续重定向到 `/scenario-analysis`，防止已有书签失效。被移除的三个页面地址统一重定向到实验配置或实验记录，不显示空白页面。

### 2.1 需要删除的提示性内容

实施时从运行界面中删除以下内容及其关联视觉组件：

- 场景与异构分析页顶部关于域间偏移不可调的说明。
- 场景与异构分析页中的信息提示条。
- 四域特征空间投影卡片及“处理前/处理后”切换。
- 域间特征距离矩阵卡片。
- 域分布摘要中的“域间偏移只读”标签。
- 实验配置页顶部关于实验互斥和单机角色模拟的说明。
- 实验配置页底部关于三级分层聚合只影响展示的注释。
- 实验记录页顶部现有的检索、脱敏导出和结论摘要说明。
- 应用顶栏中与真实任务状态无关的固定“运行中”标签、固定轮次和“暂停演示”按钮。
- 导航中的隐私攻击效果、对照分析和系统设置入口。

删除提示不改变底层边界：训练仍为单机模拟；隐私与后门仍互斥；三级结构仍只用于前端展示。这些约束由配置校验和后端任务编排保证，不再作为反复出现的页面说明。

### 2.2 旧文档处理

`FRONTEND_DESIGN.md` 和 `NEXT_PHASE_OPTIMIZATION_DESIGN.md` 作为历史设计保留，但在文件首部增加“历史版本，不作为下一阶段实现依据”标记，并链接到本文档。`README.md` 更新为当前可用页面、启动方式和后端联调方式，不再宣称三个暂缓页面可用。

## 3. 场景与异构分析页

### 3.1 页面定位

该页只完成两件事：

1. 设置基础异构环境。
2. 检查四个域中全部客户端的样本数量和类别分布。

四个域固定对应数字孪生域、战术符号域、装备数据库域和实景侦察域，每域默认 15 个逻辑客户端。域名是面向用户的显示名称，后端数据键继续使用数据集的四个域键。

### 3.2 可配置项

- 数据集：本阶段固定为 OfficeHome，组件保留数据集扩展能力。
- 每域客户端数：默认 15；若当前后端只支持固定数量，则显示为不可编辑的数值，不附加解释性提示。
- 狄利克雷参数 `alpha`：调节域内标签分布不均程度。
- 随机种子：保证数据划分可复现。
- “预览分布”和“应用到实验配置”两个明确动作。

“应用到实验配置”后把场景快照编号、`alpha`、随机种子和划分版本写入全局草稿；实验启动时提交快照编号，避免预览和实际训练采用不同划分。

### 3.3 展示内容

- 四域总样本数、客户端数、最小/最大客户端样本数。
- 按域切换的 15 个客户端样本数量柱状图。
- 客户端 × 类别的数量/比例热力图。
- 客户端明细表：编号、样本数、域内占比、类别覆盖、主要类别及占比、标签熵。
- 客户端抽屉：全部类别的样本数量和类别比例。
- 当前已应用方案与待应用方案的分布摘要差异。

本页不再展示特征空间投影、域间距离或域间偏移分析。调整 `alpha` 只触发客户端划分预览刷新。

## 4. 实验配置页

### 4.1 页面结构

页面改为“实验类型—基础参数—类型参数—校验与启动”的单页分区表单。右侧保留运行摘要，但所有摘要都来自当前表单，不使用固定演示文案。

实验类型只能选择一个：

- 异构协同实验。
- 隐私保护实验。
- 后门攻防实验。

切换实验类型时清空上一类型的专属字段，避免隐藏字段进入请求体。前后端使用同一份判别联合配置结构进行互斥校验。

### 4.2 公共参数

| 参数 | 建议控件 | 校验 |
| --- | --- | --- |
| 实验名称 | 文本框 | 1～80 字符，同一次提交不可为空 |
| 场景快照 | 只读摘要 + 返回修改 | 必须存在且未失效 |
| 聚合/训练方案 | 下拉框 | FedAvg、FedProx、异构解决方案 |
| 全局轮次 | 数字框 | 1～500 |
| 客户端参与比例 | 百分比 | 大于 0 且不超过 100% |
| 本地训练轮次 | 数字框 | 1～50 |
| 批大小 | 下拉或数字框 | 后端能力接口允许的值 |
| 学习率 | 科学计数数字框 | 大于 0 |
| 随机种子 | 数字框 | 非负整数 |
| 运行设备 | 下拉框 | 由后端能力接口返回 CPU/CUDA 可用情况 |

当选择 FedProx 时展示近端项系数 `mu`；选择 FedAvg 时不提交该字段；选择异构解决方案时展示第一阶段和正常联邦训练阶段的重要白名单参数。前端不得提供任意配置键输入框，后端也只接受白名单字段。

### 4.3 异构协同实验

支持 FedAvg、FedProx 和异构解决方案三种运行方案，用于相同数据划分下的独立实验。每次启动只运行一种方案，对照通过多次实验记录完成，本阶段不创建独立对照页面。

异构解决方案的重要参数首批只开放：

- 第一阶段本地轮数。
- 第一阶段特征摘要批大小。
- 本地扩充比例或目标样本上限。
- 正常训练阶段全局轮次、本地轮次、学习率和客户端参与比例。
- 可复现随机种子。

参数名称和范围以后端能力接口为准，前端根据 schema 动态禁用不适用字段。

### 4.4 隐私保护实验

隐私保护实验用于启动有防御或无防御的隐私攻击评估。每个任务选择一种攻击类型和一种保护状态，避免多个结果共享同一个成功率：

- 成员关系推断。
- 属性推断。
- 数据重建。

保护状态为“关闭”或“启用本地自适应保护”。启用后开放裁剪阈值、目标分位数、噪声强度、预算上限等后端支持的参数。无防御与有防御是两个独立实验，可以在实验记录中通过相同场景快照和随机种子辨认，但本阶段不生成对照分析页。

隐私保护实验请求中不允许出现恶意客户端、后门注入或攻击防御字段。

### 4.5 后门攻防实验

后门攻防实验用于启动有防御或无防御的后门攻击实验。配置包括：

- 攻击类型。
- 恶意客户端比例，或从 60 个客户端中选择明确编号。
- 目标类别、攻击起始轮次、注入比例和攻击强度。
- 攻击防御状态：关闭或启用。
- 启用防御时分别设置特征统计阶段和正常训练阶段的检测开关及白名单参数。

后门任务请求中不允许出现隐私攻击类型、噪声、裁剪或隐私预算字段。页面中的恶意节点是真值标注；运行监控中的可疑/过滤状态是防御输出，两者不能混为一个字段。

### 4.6 配置导出

“导出配置”按钮在前端校验通过后生成版本化 JSON 文件，内容与创建实验的请求体一致，并包含：

- `schemaVersion`。
- 场景快照编号和数据划分参数。
- 实验类型。
- 公共参数与该类型专属参数。
- 生成时间，但不包含实验结果。

导出前移除浏览器状态、绝对文件路径、访问令牌和未启用模式的隐藏字段。后续可增加“导入配置”，但不列入本阶段验收范围。

### 4.7 启动与停止流程

```text
填写配置 → 前端校验 → 后端预检 → 创建任务
         → 返回 experimentId → 自动进入运行监控
```

创建成功后必须使用后端返回的实验编号跳转至 `/experiments/{experimentId}/live`，不再使用 `demo` 固定编号。重复点击启动时，通过提交幂等键避免创建两个相同任务。

停止实验只在运行监控页执行。用户确认后调用停止接口，按钮进入“停止中”，后端完成当前安全检查点后把状态改为 `stopped`。停止是终态，不等同于浏览器端暂停动画。

## 5. 运行监控页

### 5.1 页面原则

监控页只展示当前实验真实返回的数据。数据连接断开时保留最后快照并显示“正在恢复连接”，不得继续生成模拟轮次。通过快照和事件序号解决刷新、断线重连及重复事件问题。

页面根据实验类型动态显示指标，避免异构、隐私和后门指标同时堆在一张图中。

### 5.2 公共区域

- 实验名称、编号、类型、方案、状态和数据源。
- 当前阶段、当前轮次、总轮次、已用时间和进度。
- 启动时间、最近更新时间、运行设备。
- 停止实验按钮；完成、失败或已停止后禁用。
- 训练/验证损失、全局准确率和学习率趋势。
- 四域准确率、最弱域准确率和域间性能差距。
- 节点状态表：客户端编号、域、当前状态、样本数、进度、最新指标、是否恶意、风险判断。
- 事件流：阶段开始/结束、轮次完成、异常、过滤、重连和任务终态。
- 地图数据流：中央节点、四个域子服务器和客户端状态的展示投影。

### 5.3 异构协同指标

- 第一阶段进度、有效特征摘要数、本地扩充样本数。
- 正常训练轮次、全局准确率、各域准确率、最弱域准确率。
- 域间性能差距、客户端参与数和掉队客户端数。
- 每域/每客户端样本分布快照链接。

### 5.4 隐私保护指标

- 任务准确率和保护带来的效用变化。
- 当前攻击的主指标：成功率、AUC 或重建误差，由攻击类型决定。
- 客户端裁剪比例、实际裁剪阈值、实际噪声强度。
- 预算消耗，仅在后端能提供严格定义时展示。
- 攻击评估阶段、目标样本数、完成比例和失败原因。

本阶段不做样本级隐私效果画廊，也不生成有防御/无防御的并排页面。

### 5.5 后门攻防指标

- 干净样本准确率。
- 攻击成功率。
- 检测真阳性率、假阳性率。
- 每阶段可疑客户端数、过滤客户端数。
- 客户端风险分数分布和恶意真值标记。
- 第一阶段与正常训练阶段各自的检测状态和事件。

### 5.6 任务状态机

```text
created → validating → queued → running → completed
                           ├──→ stopping → stopped
                           └──→ failed
```

前端按钮严格依据后端状态启用。`completed`、`stopped` 和 `failed` 都是终态；网络断开只是连接状态，不得擅自把任务标成失败。

## 6. 实验记录页

该页用来记录每次实验，不承担对照分析或独立报告生成。默认按创建时间倒序展示：

- 实验编号和名称。
- 实验类型与运行方案。
- 创建时间、开始时间、结束时间和耗时。
- 当前/最终状态。
- 数据集、场景快照、`alpha` 和随机种子。
- 最终全局准确率，以及当前实验类型的一组核心结果。
- “查看运行记录”和“查看配置”操作。

任务创建后立即产生记录；任务停止、失败或完成后更新同一条记录。刷新浏览器后记录仍由后端返回，不依赖前端本地存储。配置详情使用只读结构化视图，本阶段不提供报告检索、报告导出或自动结论摘要功能。

## 7. 前后端契约

### 7.1 建议接口

| 方法 | 接口 | 用途 |
| --- | --- | --- |
| `GET` | `/api/capabilities` | 返回设备、数据集、实验类型、参数 schema 和范围 |
| `POST` | `/api/scenarios/preview` | 预览四域客户端划分 |
| `POST` | `/api/scenarios` | 固化场景快照，返回 `scenarioId` |
| `POST` | `/api/experiments/preflight` | 校验配置和运行环境，不创建任务 |
| `POST` | `/api/experiments` | 创建并启动任务，返回 `experimentId` |
| `GET` | `/api/experiments` | 获取实验记录列表 |
| `GET` | `/api/experiments/{id}` | 获取实验元数据、配置和最终摘要 |
| `POST` | `/api/experiments/{id}/stop` | 请求安全停止任务 |
| `GET` | `/api/experiments/{id}/snapshot` | 获取最新完整运行快照 |
| `GET` | `/api/experiments/{id}/events` | 通过 SSE 订阅增量事件 |
| `GET` | `/api/experiments/{id}/metrics` | 按轮次或时间窗口补取指标 |

所有写接口返回统一错误结构：`code`、`message`、`fieldErrors`、`requestId`。前端只展示可操作的错误信息，详细堆栈留在后端日志。

### 7.2 创建实验请求骨架

```json
{
  "schemaVersion": "1.0",
  "idempotencyKey": "uuid",
  "name": "实验名称",
  "type": "heterogeneity | privacy | backdoor",
  "scenarioId": "scenario-id",
  "common": {
    "method": "fedavg | fedprox | heterogeneous_solution",
    "rounds": 30,
    "localEpochs": 2,
    "participationRate": 0.8,
    "batchSize": 32,
    "learningRate": 0.001,
    "seed": 20260816,
    "device": "cpu"
  },
  "heterogeneity": {},
  "privacy": null,
  "backdoor": null
}
```

三个专属配置字段中必须且只能有一个与 `type` 对应，其余为 `null`。后端把请求转换为配置白名单，不允许前端直接指定 Python 模块、命令、输出目录或任意启动参数。

### 7.3 事件模型

事件至少包括：

- `experiment.started`、`experiment.stopping`、`experiment.stopped`。
- `stage.changed`、`round.started`、`round.completed`。
- `client.status.changed`、`client.metric.updated`。
- `metric.updated`、`warning.raised`。
- `experiment.completed`、`experiment.failed`。

每条事件必须包含 `experimentId`、单调递增的 `sequence`、`timestamp`、`type` 和 `payload`。SSE 使用 `Last-Event-ID` 或 `afterSequence` 续传；若事件已过期，前端先拉取快照，再从快照序号继续订阅。

## 8. 后端单机任务层

新增轻量 API 与任务管理层，封装现有单机运行入口，不改造成真实网络客户端：

1. API 进程接收并校验白名单配置。
2. 每个实验由独立子进程执行，避免训练阻塞 API 和事件推送。
3. 子进程通过进程安全队列上报事件和指标。
4. 任务管理器维护状态、PID、停止信号、最新快照和事件序号。
5. 结果写入实验目录中的版本化 JSON，不把模型权重塞入 API 响应。
6. 停止时先发协作式停止信号；超时后的强制终止只作为受控兜底，并记录原因。

同一时间默认只允许一个 GPU 实验；CPU 并发数由能力接口返回。服务重启后把遗留的 `running` 记录恢复为 `failed` 或可识别的 `interrupted`，不得一直显示运行中。

## 9. 文件级实施清单

### 9.1 前端修改

- `src/App.tsx`：删除三个暂缓页面的懒加载和路由。
- `src/components/AppShell.tsx`：精简导航；顶栏改为真实任务摘要，移除演示控制逻辑。
- `src/pages/ScenarioAnalysisPage.tsx`：删除域间特征图表和提示，仅保留环境设置与客户端分布。
- `src/pages/ExperimentPage.tsx`：改为类型安全的真实表单，接入预检、导出和启动。
- `src/pages/LiveMonitorPage.tsx`：按实验编号获取快照、订阅事件、展示动态指标并支持停止。
- `src/pages/ReportsPage.tsx`：改为后端实验记录列表和只读详情。
- `src/store/useAppStore.ts`：拆分全局 UI 状态、配置草稿和运行任务状态，删除固定 `demo` 状态。
- `src/types.ts`：增加配置判别联合、任务状态、模式指标和 API 响应类型。
- `src/api/scenarioAdapter.ts`：增加场景固化接口，后端模式下禁止静默回退模拟数据。
- `src/api/trainingAdapter.ts`：补齐停止、任务详情、指标补取和终态事件，保留 SSE 恢复能力。
- `src/styles/index.css`：删除三个页面及固定特征图表样式，补充配置错误、连接状态和模式指标布局。
- `src/test/*`、`tests/e2e/*`：更新为真实契约的模拟服务器测试。

### 9.2 前端删除

- `src/pages/PrivacyPage.tsx`。
- `src/pages/ComparePage.tsx`。
- `src/pages/SettingsPage.tsx`。

若这些页面的专用 mock、类型和 CSS 选择器已无其他调用，一并删除；公共图表、指标卡片和节点详情组件继续复用。

### 9.3 前端新增建议

- `src/api/experimentApi.ts`：创建、停止、列表、详情和预检。
- `src/features/experiment/configSchema.ts`：前端 schema 与跨字段校验。
- `src/features/experiment/configExport.ts`：生成版本化配置文件。
- `src/features/monitor/metricRegistry.ts`：按实验类型映射指标和图表。
- `src/features/monitor/runtimeReducer.ts`：快照与事件归并。
- `src/features/reports/ExperimentDetailDrawer.tsx`：实验记录详情。

### 9.4 后端新增建议

- `federatedscope/standalone_api/app.py`：HTTP/SSE 入口。
- `federatedscope/standalone_api/schemas.py`：请求、响应和配置白名单。
- `federatedscope/standalone_api/task_manager.py`：任务状态机和进程管理。
- `federatedscope/standalone_api/runner.py`：把统一配置转换为现有单机实验入口。
- `federatedscope/standalone_api/events.py`：事件序号、快照和 SSE 广播。
- `federatedscope/standalone_api/repository.py`：实验记录和结果摘要持久化。
- `federatedscope/standalone_api/metrics.py`：把现有训练日志/监控值规范化为前端指标。

具体 Web 框架在实施前以当前环境已安装依赖为准；优先减少新依赖，不为 API 层改写训练核心。

## 10. 分阶段实施顺序

### 阶段 A：页面收敛与契约冻结

- 删除暂缓页面、导航、路由和提示性内容。
- 精简场景与异构分析页。
- 定义配置、任务、事件、指标 schema 和错误码。
- 更新 README 和历史设计文档标记。

完成标准：前端仅出现五类入口，所有页面可以在纯前端模式下构建，但不再把模拟数据标为真实结果。

### 阶段 B：单机任务 API

- 实现场景预览/固化、能力、预检、创建、停止、详情和列表接口。
- 封装现有单机运行器并建立子进程任务管理。
- 持久化任务状态、规范化配置和最终摘要。

完成标准：可以使用 HTTP 创建一个最小实验、观察状态变化、停止实验并在服务重启后读取记录。

### 阶段 C：实验配置真实联调

- 完成三种实验类型的动态表单和跨字段校验。
- 接入 FedAvg、FedProx 与异构解决方案参数。
- 完成配置 JSON 导出、预检和防重复提交。
- 启动成功后自动跳转到正确的实验监控地址。

完成标准：三类请求均只含本类型字段；后门模式不会启用隐私功能；非法参数在启动前得到字段级提示。

### 阶段 D：实时运行监控

- 接入真实快照、SSE、断线恢复和指标补取。
- 按实验类型渲染公共指标和专属指标。
- 接入停止流程和所有终态展示。
- 将后端逻辑客户端事件投影到三级地图，但不改变后端聚合方式。

完成标准：刷新或短暂断网不会丢轮次；停止后不再产生新训练轮次；页面没有自动生成的伪实时数据。

### 阶段 E：实验记录

- 接入实验列表、详情和最终摘要。
- 支持从记录打开仍在运行的监控或已结束的只读监控。
- 完成空状态、失败任务和中断任务展示。

完成标准：每次创建实验都有且只有一条可持久读取的记录，状态与监控页一致。

### 阶段 F：测试与收尾

- 完成单元、接口、集成和端到端测试。
- 删除失效 mock、样式、类型和依赖。
- 更新部署与联调说明。

## 11. 测试计划

后端测试统一使用：

```bash
/root/.local/share/mamba/envs/pfedba/bin/python -m pytest
```

### 11.1 前端单元测试

- 三种实验配置的判别联合和字段清理。
- FedProx `mu` 的条件显示与校验。
- 隐私/后门字段互斥。
- 配置导出与实际请求体一致。
- 快照 + 事件归并、序号去重和终态保护。
- 不同实验类型的指标注册和缺失值处理。

### 11.2 后端接口测试

- 配置白名单、范围和跨字段校验。
- 幂等启动不会产生重复任务。
- 创建、运行、停止、完成和失败状态转换。
- 停止隐私、后门、异构三类最小任务。
- SSE 事件顺序、断点续传和快照恢复。
- 实验记录在 API 重启后可读取。
- 后门请求拒绝隐私字段，隐私请求拒绝后门字段。

### 11.3 集成与端到端测试

- 设置 `alpha` → 固化场景 → 创建实验 → 自动跳转监控。
- FedAvg、FedProx、异构解决方案分别可启动。
- 隐私攻击在有防御和无防御下分别可启动。
- 后门攻击在有防御和无防御下分别可启动。
- 监控页接收真实轮次、客户端状态和模式指标。
- 运行中停止 → 状态变为已停止 → 实验记录同步更新。
- 刷新监控页和 SSE 断线重连后指标不重复、不倒退。
- 被移除的页面不再出现在导航中，旧地址不会渲染旧页面。

### 11.4 回归测试

- 首页地图仍全屏展示四域、每域 15 个节点、域子服务器和中央服务器。
- 场景页仍能展示 60 个客户端的样本数、类别计数和类别比例。
- 现有后端核心测试继续在指定环境通过。
- 前端执行 `npm test`、`npm run build` 和 `npm run test:e2e`。

## 12. 验收标准

1. 导航只包含综合态势、场景与异构分析、实验配置、运行监控入口语义和实验记录；运行监控通过真实实验编号进入。
2. 场景页不再出现域间特征投影、距离矩阵或相关提示，能够设置 `alpha` 并查看四域 60 个客户端分布。
3. 实验配置可选择 FedAvg、FedProx 和异构解决方案，并能配置三类实验所需的重要参数。
4. 隐私实验和后门实验请求严格分离，后门任务不执行隐私攻击或隐私保护。
5. 配置可导出为版本化 JSON；启动成功自动进入对应运行监控页。
6. 前端可以真正启动和停止后端单机任务，状态以服务端为准。
7. 监控页实时显示异构、隐私或后门实验的适用指标，不以定时器伪造真实结果。
8. 每次实验都形成持久化记录，刷新或重启后仍可查看。
9. 隐私攻击效果、对照分析和系统设置三个独立页面不存在于下一版导航和路由中。
10. 后端核心测试与前端测试均通过，且不要求真实分布式客户端。

## 13. 本阶段不实现

- 真实分布式客户端或跨机器通信。
- 后端真实两级聚合；中央—域子服务器—客户端只做前端态势投影。
- 独立隐私攻击效果页面。
- 独立有无防御对照分析页面。
- 系统设置页面。
- 多种隐私攻击在同一个任务中并行执行。
- 隐私与后门在同一个任务中组合执行。
- 自动实验结论、报告生成和报告导出。
- 任意后端配置键、命令或 Python 模块的前端输入。
